我做这个项目，首先是为了掌握 Agent 的执行机制，并探索它在企业内部的应用可能性。现阶段实现了工具调度、文件检索、任务规划和上下文管理。后续业务落地会聚焦需求变更影响分析，将项目资料、团队分析规范和项目管理系统接入同一流程。是否值得继续自研，要看它能否减少顾问反复整理上下文和跨系统操作的工作，并通过真实案例验证分析质量。





# Dodo-Agent 通用 AI Agent 平台

Dodo-Agent 是一套基于 Spring AI 生态构建的通用 AI Agent 平台，采用多 Agent 协作架构，支持联网搜索、文件 RAG 问答、深度研究、内部OA系统MCP能力集成 等场景。系统围绕 Agent 运行时、上下文管理、Tool Calling、MCP、RAG 检索增强及任务调度等能力进行建设，实现从任务规划、工具调用、知识检索到结果生成的完整 Agent 执行链路。

技术栈： Spring Boot 3.x、Spring AI、PostgreSQL + pgvector、MySQL、Redis、Redisson、Project Reactor、MCP、MinIO、Tavily

责任描述： 负责 AI Agent 平台核心能力设计与开发，承担 Agent 架构、深度研究引擎、上下文压缩、Tool Calling、RAG 检索增强、分布式任务管理及 PPT 生成流程等模块建设，推进 Agent 公共能力抽象与运行机制优化。



设计并实现 ReAct Agent 四段式流式交互：实时输出思考、答案、联网检索来源与智能追问，并支持会话持久化与历史还原。



多 Agent 架构设计： 围绕联网搜索、文件 RAG、PPT 生成、深度研究、技能增强等场景设计多种专用 Agent，抽象 `BaseAgent` 统一管理会话上下文、SSE 流式输出、任务注册与取消等公共能力，降低不同 Agent 的重复开发成本。



Agent 上下文压缩机制：针对多轮 Tool Calling 导致上下文持续膨胀问题，设计 `micro_compact + auto_compact` 两级压缩策略，对历史 Tool 响应及超长参数进行轻量压缩，并在 Token 超过阈值后触发 LLM 结构化摘要，结合 Token 估算与消息截断降级机制控制上下文长度。



分布式任务管理： 基于 Redis 与 Redisson 构建 Agent 任务管理机制，通过分布式锁保证同一会话任务单实例执行，使用 `RTopic` 实现跨实例任务取消通知，并结合 TTL 定时续期解决长耗时 Agent 任务锁提前失效问题，支持服务水平扩展。



RAG 检索增强优化： 构建 Query 改写、多查询扩展、向量检索及结果去重的完整 RAG 检索链路，通过原始 Query + 多路扩展 Query 提升知识召回能力，并基于 pgvector 元数据过滤实现按文件 ID 精确范围检索。



MCP 与 Skills 插件体系：基于 Spring AI MCP Client 接入 Tavily 等外部工具服务，同时设计 `SKILL.md` 技能规范，通过运行时扫描注册和按需加载领域提示词，将不同领域能力以插件形式接入 Agent，降低新增 Agent 能力的扩展成本。





# 任务管理

## 单机实现

```
任务注册与并发控制
控制同一个会话只能在同一时间存在一个任务，否则输出会错乱

管理流的句柄Disposable
Disposable disposable = chatClient.prompt()，控制句柄可以控制流的输出停止。

完整生命周期
1、根据会话id判断是否存在任务，存在就直接返回提示
2、任务注册
3、开启大模型对话后，注册句柄到任务上
4、如果用户从前端取消对话，则取出句柄进行关闭流释放资源
5、一切处理完后删除任务

通过引入 AgentTaskManager，系统能够对每一次智能体执行任务进行统一管理，实现任务注册、并发控制、流式终止以及资源释放。
```

## 分布式实现

```
redis分布式锁处理并发、redis存储任务、mq广播消息给所有实例清理任务
```

# 四段式输出

## 实现原理

```
基于react agent的循环架构，每次获得chunk就进行解析reasoningContext部分，并实时用flux推送到前端展示、
中间大模型正式回答时进行正文推送、如果调用了tavily，则根据返回的信息，构建相关连接的信息推送，最后再调用一个大模型传递当前的消息上下文，让大模型做一次问题推荐，最终输出给前端
```

## 模型思考问题

```
minimax 嵌入content 中，XML 标签分隔 <think>标签处理的，我们做了手动的处理
```

# 文件问答功能实现

## 初版针对小文件

```
第一版做简单的提示词注入文件内容
当前对话上传的文件，在与大模型发起正式对话前，先做文件读取、多模态识别，然后将解析的内容直接注入的提示词中，给大模型，大模型进行总结，这样的优势就是速度快，并且大模型少一步思考，适合小文件小于10000字符。

只应用于当前对话，跨会话不支持。
```

## 优化版针对大文件

```
考虑到公司内部还是有这样的需求产品就提了优化

我的实现就改成了通过toolCall的方式，定了一个analyzeFile文件解析x工具、里面使用多模态识别和RAG技术处理，当模型决策需要读取文件信息的适合会调用工具，针对图片就走多模态识别qwen-vl-max，针对文本就走RAG检索qwen-text-embedding-v4，工具返回文件的信息给大模型，然后大模型再决策或者直接输出。

工具定义toolCall参数文件id、用户提问

RAG简单的处理，问题改写、查询扩展、元数据过滤、然后进行向量检索，然后通过工具返回结果给大模型

文件上传，目前只做简单的文件限制10M以内文本格式、pdf、txt、word、图片等。
上传时图片不解析，文本采用tika读取文档判断文件的字符的个数，大于取10000，做向量化，小于的直接存储
做向量化的文档要进行tika的文档读取，然后并进行文档分片固定500，50切分，然后进行向量化。

用户发起提问的时候，如果当前会话有文件，那就会进行提示词的动态注入，动态告诉大模型当前有哪些文件，顺序是怎样的。


将rag封装为工具、基于react架构，大模型可以自己决策需要怎么调用工具、调用几次工具，怎么整合数据等，这是一个非常优秀的实战，agentic rag而不是workflow rag
```



# Skills 插件体系



## 初版

```
集成实现Skills 插件体系

一开始我是参考claude code源码中对skill的实现进行的开发

存储方式：skill基于数据库与文件存储

上传流程：用户可以上传skill的zip到agent中，我会进行解压，然后最多向上遍历3层目录找到SKILL.md，然后解析
name+desciption、如果数据库中以存在同名的，会先做数据库删除与本地文件删除，然后将新的导入进去，用户可以再web界面启用和禁用skill。

自动发现:我们基于SkillManager这个组件管理，组件初始化的时候，同步数据库与skill文件夹目录的数据，以文件夹的数据为准

动态生效:每次创建reactagent的时候组装skill给agent

怎么实现接入大模型：我会将所有初始化的skill，提取name+descrption并拼接系统提示词直接给到大模型，然后定义
ReadSkill方法，模型发现需要使用skill的时候会自动调用ReadSkill方法传入skill名称获取到skill的全部内容
```

## 重构版

```
以上方案在实际应用与调试的过程中，很受大模型的能力影响，算力差的模型例如qwen-flash经常把skill当作tool直接去调用一个不存在的tool，所以我在提示词中加了很多约束、few-shot等。

后来基于spring-ai-agent-utils 重构了skill的实现

后面偶然间看到了spring-ai的这个工具集的实现但是他依赖spring ai 2.0

Spring AI 的做法是：把所有技能的元数据注入到一个统一的 SkillsTool 工具的描述中，模型只需要调用这一个工具，传入技能名称即可。

方案对比优劣势就是出版的话技能列表和获取技能是分开的，新版就是技能列表和获取技能是合二为一的，解决了算力差的模型的幻觉问题。
```



# 会话压缩实现

```
react agent尤其是在调用agent skill的时候，由其是处理长链路的复杂任务的时候会约跑约笨 lost in the middle

参考了claude code的实现,又借鉴了一下JVM YoungGC和FullGC的特点

我的agent分为两层压缩机制
micro_compact 每轮对话都执行做占位符替换
auto_compact token超阈值时触发的重量级压缩，用 LLM 摘要替换所有旧消息
```



## micro_compact实现

```
micro_compact实现：
为什么需要micro_compact呢，因为长任务执行中一轮对话中间会触发多次工具调用，例如写代码生成来说吧，需要调用readfile读取当前代码结构，然后规划写入步骤，然后一步步写，然后思考验证一下代码读取一下，然后继续写，这样一次对话中间经过很多轮的工具调用，读取跟写入会生成很多的上文ToolCall、ToolResponse信息


我们会给这个压缩器初始化一些配置
保留4轮工具调用、默认工具压缩阈值200字符(有些返回的信息很短就没必要压缩了)、默认token阈值60000、默认保护的工具(例如SKILL不能被压缩)

micro_compact每次都在对话开始前进行压缩相当于youngGC，为什么在对话前是因为我要在发起对话前就处理好上下文


token估算，因为我们是在大模型调用前处理压缩，所以不好太好拿具体的token消耗数量，我们采用估算的方案，简单点，

开始真实的压缩，记录好工具的调用的记录，保留最近的4个，并跳过保护的tool，替换toolCall、toolResponse的内容改为json信息，记录已经被压缩。



遇到的问题：
遇到的一些问题，占位符必须是JSON，自然语言不行。



micro_compact 的效果估算
假设一个 Agent 执行了 10 轮工具调用，每轮产生平均 2000 字符的 ToolResponse 内容。在 keepRecentTools=4 的情况下：
压缩前：10 轮 × 2000 字符 = 20000 字符（约 13300 个 token for CJK）
压缩后：6 轮 × 约 80 字符（JSON 占位符）+ 4 轮 × 2000 字符 = 8480 字符（约 5650 个 token）
节省约 57% 的上下文空间
```



## auto_compact实现

```
micro_compact 虽然说能节约一部分上下文，但是他毕竟还是只是有一定的局限性，他只是将大内容替换成小内容，并没有解决消息数量的问题，当消息数量越来越多，依然还存在上下文膨胀的风险，所以auto_compact该出场了。

auto_compact的逻辑是将用户的当前提问之前的所有消息全部压缩，只留下摘要。这意味着 auto_compact 是有损压缩，摘要必然丢失一些细节。所以不能频繁触发，每次触发都应该是在不得不压缩的时候。这和 Full GC 的逻辑是类似的。


中间遇到的问题：

一、到底怎么切分
一开始想的是精确切割，比如保留最近几条完整的消息，其余的去生成摘要，但遇到消息切分的一个问题，假如保留最近4条消息，如果切到了toolresponse，那在大模型中toolresponse必须跟在toolCall的后面，这样编码可以解决但比较复杂，即使能处理这种情况，那就会存在半压缩、半原文的 消息，大模型理解这种断层的消息也会出现偏差、所以我索性就直接一道切，将当前提问前的所有消息、除了系统提示词都进行压缩


二、摘要需要知道当前任务是什么
前面说了我将除了当前提问之前的所有消息进行压缩，没有包含当前的提问，这时候生成摘要无法判断哪些信息跟当前的提问比较重要，这也是个问题，所以的话，我将当前提问信息也加入了提示词中让大模型一起生成摘要，这样摘要会更有侧重
保持任务的延续性


三、token阈值设置比较合理

设置太小，触发频繁，上下文的精度就会降低，增加延迟与api成本
设置太高，基本不会触发，如果等到下一轮该触发的时候，可能上下文直接爆了，或者还没爆之前模型就lost in the
middle,已经开始出现幻觉了。

所以我们应该设置最大token数居中的值，比如128k那就是6w就是合理的阈值。
```

