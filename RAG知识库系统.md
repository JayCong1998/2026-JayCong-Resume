# 企业问题知识库

公司的制度规定文件

员工考勤管理制度、公司福利管理规定、信息安全管理制度、员工绩效管理制度

系统的一些操作指引等

OA系统、工时系统、内部邮箱操作、招聘系统、SAP、客户管理



```
我是公司 AI 研发中心搭建的制度问答机器人，专门回答公司内部制度问题。
• 您可以向我咨询差旅报销标准、考勤规则、假期政策、系统操作指引等与公司内部制度相关的问题
• 涉及个税法规、编程、闲聊等不在公司制度范围内的问题，我无法解答
• 重置密码、开通权限、更换工资卡等需要人工办理的操作，我暂时无法代办，可以引导您联系对应窗口的同事协助处理

本回答由 AI 根据公司制度文件整理，以人工客服为准。如需转人工客服请输入"转人工"。
```







# 分块设计



## 父子分块

父分块完整的数据存数据库并跳过向量存储，子分块元数据记录父分块id并进行向量化，向量化检索到的该分块的时候从元数据从取出parentChunkId，并从数据库中查询到完整的文档片段并返回

```
CHUNK_ID   分块id
PARENT_CHUNK_ID  父分块id
```



## 兄弟分块

将完整的一个片段拆成多个兄弟分块，所有分块都要进行向量化，记录同一个兄弟分块组brothChunkId，并记录每个文档的自己索引编号、比如0,1,2,3,4，并记录总的分块数量例如就是5。

当向量检索到某个分块的时候

```
CHUNK_ID   分块id
BROTHER_CHUNK_ID   兄弟分块组id
BROTHER_CHUNK_INDEX   兄弟分块组索引编号
BROTHER_CHUNK_TOTAL    兄弟分块一共多少个
```









# 数据存储设计







## 关系型数据库

个人绩效、个人考勤、个人工时填报、个人绩效等



## 图数据库





## 向量数据库





# 索引构建







# 多版本管理







# 权限设计







# 文档解析

自建的mineru服务，因为框架提供的文档读取器都不太好用，左右格式读不出来



# 发起对话



根据前端是否传了会话id判断

创建会话数据生成唯一id

启用虚拟线程调用大模型使用轻量模型qwen3.5-flash

快速响应生成对话摘要标题，更新标题。

并将会话唯一id以约定的格式返回给前端



# 意图识别

提示词工程、角色定义、few-shot、结构化输出

可加入槽位提取、提取相关的信息出来

前置rag，泛化意图知识库，实现更复杂





# 意图定制化提示词





# 问题澄清









# 模块化RAG



QueryTransformer查询改写      基于大模型将问题改写成   

QueryRouter查询路由             查询路由器，基于大模型判断

ContentRetriever内容检索      检索器 向量检索、关系型数据库检索、向量数据库检索

ContentAggregator内容聚合      重排序rrf rerank

ContentInjector内容注入        提示词增强，将原始内容注入到用户原始消息中





# 向量检索、关键字检索





# 关系型数据库检索





# 图数据库检索

自定义实现了包装了一层KnowEngineNeo4jContentRetriever检索器

内部做了降级处理，如果图数据库没有检索到数据或者检索异常了，降级继续查询向量数据库。



我们做的比较简单直接基于提示词将图数据库的表结构直接灌输给大模型，最后做一个兜底就行了。
我们毕竟是做一个rag系统，而不是chatbi或者dataagent系统

# 查询改写



在RAG的**相似度检索**中，检索效果的好坏，很大程度取决于用户问题的表达方式。用户输入的提问是不可控的，**往往存在模糊、信息缺失、上下文依赖**等问题，难以直接高效地匹配到高质量的相关文档块。因此需要在检索前对用户输入的查询进行优化**，从而显著提高检索的准确率和最终生成答案的质量**。



1、分解：

分解是指将用户的**复杂、多步骤或包含多个子问题**的查询，拆解成若干个**相互独立、更简单、更具体**的子问题。每个子问题可以独立进行检索，最终将多个检索结果合并，用于回答原问题。  



2、富华

富化是指在原始查询中**添加上下文信息、背景知识或必要的限制条件**，以消除歧义、补充缺失的信息，使查询更完整、更具体。



3、多样化

多样化是指对同一个用户查询生成多个**多个语义相近或相关**的变体，以提升对知识库内容描述多样性的覆盖，从而增强召回率。主要应对以下情况：

1. 描述风格不一致：文档的表述方式不同，如有的是“接口性能优化”，有的是“提升接口响应速度”。
2. 用户表述差异性：不同用户的表达方式习惯不同。



4、回溯提示

所谓回溯提示，是从Step-Back Prompting 翻译过来的，它的核心思想是**先引导模型“后退一步”，从具体问题中抽象出更一般的原理、概念或背景知识；再基于这些抽象信息进行推理或检索，最终回答原始问题。**





langchain4j默认有几个实现吧

有一个default什么都没改，原始消息直接返回回来

有一个多样性改写ExpandingQueryTransformer 将用户问题转换成多个不同的表达版本，语义相同但表达不同的

还有一个CompressQueryTransformer 基于上下文改写问题、这就是问题富华的一个手段



我自己实现了QueryTransformer，主要想将查询改写后的问题也记录到数据库中，后续作为bad case作为优化

首先先做一个问题改成1个问题的优化方案，如果效果不好再加上一对多的方案。

1对多话，整体耗时会更多一些。



所以我用到了以下几个优化手段吧

问题富华、抽象改写、错别字改写、简洁改写

# 查询路由

多RAG系统的知识来源不一定只能从向量数据库，还是可以从关系型数据库、图数据库的，所以，我们需要实现一个检索器的路由，根据用户的问题路由到不同的检索器中。



我们需要支持三种数据源路由策略：

关系型数据库 (relational_db)：适用于结构化数据查询，如车辆信息、保险信息、订单信息等

图数据库 (graph_db)：适用于实体关系查询，如车型关系、影响链、层级结构等

知识库 (knowledge_base)：适用于语义相似性查询/BM25关键词查询，如售前咨询、售后支持、技术问题等。



路由决策流程：

使用 LLM 分析用户查询语义

根据预定义的 Prompt 模板判断最适合的数据源策略

返回对应类型的 ContentRetriever 集合、也就是内容检索器



这里可以根据路由结果返回1个策略或多个策略组合起来



# 混合检索

**1、只用了向量检索效果不行**

核心问题：向量检索依赖语义相似度，对专有名词、精确短语、编号等缺乏精确匹配能力；且在面对极短查询时，因语义上下文有限，容易导致召回内容过于宽泛。

**2、只用bm25关键词检索效果不行**

核心问题：关键词匹配是死板的字面匹配，无法理解同义词、语义表达变化或复杂的自然语言意图。



项目中langchain4j提供了4种es的检索方式

近似knn查询、精确knn查询、全文检索、混合检索4中策略



开源版es、无法使用混合检索

```
[security_exception] current license is non-compliant for [Reciprocal Rank Fusion (RRF)] 
```



并且还有个问题我们用的那个版本的langchain4j，全文检索没办法传元数据过滤条件，





我们使用混合检索

重写一下es的ContentRetriever 在检索到相应的chunk之后，做父子分段的处理：

检查检索结果是否携带 parentChunkId

若有，读取父分段的完整文本

用父分段内容替换子分段，获得更完整的语义



# chunk多级缓存设计

在项目中，我们用到了父子分块，来实现更加精准的rag检索。在父子分块的检索中，我们可能会在一起检索中检索到多个子分段，但是他们对应的父分段可能是同一个。

并且这种概率其实是比较高的，因为一般一整个段落的内容都是有关联的，这样就会导致在语义相似度检索的时候都能匹配上的概率也会更高一些。、

那么就可能会出现一次检索到的多个子分段，但似乎他们对应的父分段就是同一个，这时候，我们就可以借助缓存机制，来减少针对同一个分段的多次数据库检索。

首先我们引入Redis做一层缓存，在检索chunk的时候，先从redis检索，查不到在从数据库检

另外，我们为了进一步的减少redis的请求，我们在检索器中，维护了一个map，作为本地缓存，并且这个map在方法执行结束后就释放了：



兄弟分块设计也是如此，可以根据检索到chunk获取到其中的brotherChunkId，然后继续从向量中检索到完整的文档数据。

如果考虑到不想拿到完整的文档数据，可以仅获取相邻的上下文档，比如当前分块的索引是3，那么可以获取同一个brotherChunkId下的 brotherChunkIndex是2和4的数据，拿到这3块数据后，可以再进行组装，消除其中的overlap





# 重排序

langchian4j默认支持重排序

默认实现的组件DefaultContentAggregator

但是这个默认实现有一个关键的问题，那就是他只做融合，他会把所有的检索结果都给到LLM，比如多个检索器，比如向量检索检索到5个文档，全文检索也检索到5个文档，那么RRF之后，就是会去重后保留剩余的文档例如剩8个，这样会导致内容太多了，其实模型可能根本不需要这么多文档的。



ReRankingContentAggregator

首先进行rrf排名并去重然后调用rerank模型将与问题与答案进行打分，最后保留指定条数。





项目中我们自定义了一个ReRankingContentAggregator，采用自己ONNXScoreModel，

`bge-reranker-v2-m3` (全能首选): 性价比最高。它在保持高精度的同时，模型体积小、速度快，并原生支持超过100种语言的跨语言检索，是大多数 RAG 应用的首选。





一、rrf算法

一个文档在多个排序列表中排名越靠前、出现次数越多，其融合得分就越高。

它不依赖于原始得分（如 BM25 分数、神经网络打分等），只使用**排名位置（rank）**，因此具有良好的通用性和鲁棒性。

所有文档的倒数排名加到一起最后计算



例如文档d，在第一个列表排名2，第三个列表排名3，那么就算就是   1/60+2   +  1/60+3   依次类推





二、ReRank模型

ReRank 模型是检索流程中的关键组件，作用是对初步检索得到的候选文档进行二次精细排序。它能深入理解查询与文档的语义关联，比传统检索方法更精准地判断相关性，从而提升检索结果的质量。

主要流程就是先通过向量检索、关键词检索等方式获取一批候选文档，再由 ReRank 模型对这些文档重新排序，最终输出最相关的结果。





# 引用文档返回

文档维度的RAG引用信息，用于前端展示

根据前面步骤获取到的文档，拿到元数据进行去重获取到docId以及文档名称以及地址相关的信息，通过与前端约定的格式响应给前端展示。

例如"[REFERENCE]:" + JSON.toJSONString(ragReferencesDocs)



chunk维度的RAG引用信息，用于数据持久化

根据前面步骤获取到的文档，拿到元数据进行去重获取到chunkId以及chunk内容，持久化数据库。





# 遇到的问题坑

1、模型json输出不稳定

```
json格式化修复工具
帮助我们实现json的修复。（思想参考了python中的json_repair框架）
```



2、多轮对话问题重写上下文污染

```
问题重写的时候要结合上下文进行问题重写，但是上下文中包含了之前意图识别的上下文，导致最终模型输出的问题改写不准确。

解决办法是，上下文隔离意图识别的内容不会带入到问题重写中，做了一次缓存删除
因为意图识别的对话不会存到数据库中，让大模型从新去数据库加载对话数据
```



3、langchain4j的es的全文检索默认不支持元数据过滤

```
重写了es全文检索方法
```



4、langchain4j的es混合检索默认低层会直接使用es的rrf做融合，但是开源版的es不支持这个方法

```
不走默认的混合检索，通过编排全文检索和向量检索后，实现ContentAggregator器，使用rrf+rerank模型处理
```







# RAG测评

